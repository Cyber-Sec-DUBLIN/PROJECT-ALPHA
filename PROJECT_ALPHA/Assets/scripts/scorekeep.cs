using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scorekeep : MonoBehaviour
{

//This scripts purpose is simple. Keep track of who's won a round and reset the scene based off of that. Obviously right now it ain't finished as you can clearly see
    public GameObject PLAYER1; 
    public GameObject PLAYER2; 
    static int score1;
    static int score2;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
